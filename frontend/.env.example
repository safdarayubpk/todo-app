# Better Auth Configuration
# Secret key for signing sessions (minimum 32 characters)
BETTER_AUTH_SECRET=your-better-auth-secret-at-least-32-characters-long

# Database Configuration (Neon PostgreSQL)
# Format: postgresql://user:password@ep-xxx.region.neon.tech/dbname?sslmode=require
# Note: Uses standard postgres driver (not asyncpg) for Better Auth
DATABASE_URL=postgresql://user:password@ep-xxx.region.neon.tech/todoapp?sslmode=require

# Backend API URL
NEXT_PUBLIC_API_URL=http://localhost:8000/api/v1
